#include <string.h>

//usage() prints the format for the finds function
void usage()
{
	write(1, "usage: finds -p pathname [-f c|h|S] [-l] -s string\n", strlen("usage: finds -p pathname [-f c|h|S] [-l] -s string\n"));

	return;
}
